<div class="container mt-5">
  <div class="text-center mb-5">
    <h1 class="display-4">Developer Portfolios</h1>
    <p class="lead text-muted">
      A curated collection of <span class="fw-bold"><%= @total_portfolios_count %></span> developer portfolios
    </p>
  </div>

  <%= render "letter_filters" %>

  <%= render 'portfolio_cards', portfolios: @portfolios %>
</div>
